{
  "name": "express-graph_ql-aws-dynamo_db",
  "version": "0.1.0",
  "bin": {
    "express-graph_ql-aws-dynamo_db": "bin/express-graph_ql-aws-dynamo_db.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "test": "jest"
  },
  "devDependencies": {
    "aws-cdk": "2.50.0",
    "jest": "^27.5.1"
  },
  "dependencies": {
    "aws-cdk-lib": "2.50.0",
    "constructs": "^10.0.0"
  }
}
